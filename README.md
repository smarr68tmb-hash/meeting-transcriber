# 🎙️ Meeting Transcriber

**Система записи и транскрипции совещаний для macOS**

Записывает аудио встреч (включая системный звук через BlackHole), транскрибирует с помощью Whisper, и генерирует саммари через LLM.

## ✨ Возможности

- 🎤 **Запись встреч** — микрофон, системный звук, или оба одновременно
- 🔊 **BlackHole интеграция** — запись Zoom, Google Meet, Яндекс.Телемост
- 📝 **Транскрипция** — локально (faster-whisper) или через облако (Groq API)
- 🎭 **Диаризация** — определение спикеров (кто что сказал)
- 🧠 **Суммаризация** — краткое саммари, action items, анализ через LLM
- 📊 **Мониторинг** — визуальный индикатор уровня звука в реальном времени
- 🎨 **Modern CLI** — красивый интерфейс с Rich (таблицы, панели, цветной вывод)

## 🚀 Быстрый старт

### Установка

```bash
# Клонируем
git clone <repo-url> ~/Scripts
cd ~/Scripts

# Устанавливаем зависимости
pip3 install -r requirements.txt

# Устанавливаем BlackHole для записи системного звука
brew install blackhole-2ch
```

### Использование

**Запись встречи** — двойной клик на `StartRecording.command`

**Транскрипция файла** — перетащи аудио файл на `Transcribe.command`

### Терминал

```bash
# Записать встречу
python3 -m meeting_transcriber record "Название"

# Транскрибировать файл
python3 -m meeting_transcriber transcribe file.wav

# Показать устройства
python3 -m meeting_transcriber list-devices

# Проверить BlackHole
python3 -m meeting_transcriber blackhole-status
```

## 📁 Структура

```
~/Scripts/
├── StartRecording.command    # 🎙️ Запись встречи
├── Transcribe.command        # 📝 Транскрипция файла
├── ListDevices.command       # 🔊 Список устройств
├── requirements.txt          # 📦 Зависимости
├── README.md                 # 📖 Документация
└── meeting_transcriber/      # 📂 Основной код
    ├── cli_typer.py         # Командный интерфейс (Typer + Rich)
    ├── config.py            # Конфигурация
    ├── recorder.py          # Запись аудио
    ├── transcriber.py       # Транскрипция
    ├── blackhole.py         # BlackHole интеграция
    ├── groq_backend.py      # Groq API
    ├── summarizer.py        # LLM суммаризация
    ├── audio_monitor.py     # Мониторинг уровня
    ├── postprocess.py       # Фильтрация галлюцинаций
    └── whisperx.py          # Диаризация спикеров
```

### Результаты сохраняются в:

| Тип | Путь |
|-----|------|
| Записи | `~/Meeting_Recordings/` |
| Транскрипты | `~/Meeting_Transcripts/` |
| Логи | `~/Meeting_Recordings/logs/` |

## 🔊 BlackHole — запись системного звука

BlackHole позволяет записывать звук из Zoom, Google Meet и других приложений.

### Настройка (один раз)

1. **Установка:**
   ```bash
   brew install blackhole-2ch
   ```

2. **Audio MIDI Setup** (через Spotlight):
   - Создать **Multi-Output Device** (Built-in Output + BlackHole 2ch)
   - Создать **Aggregate Device** (Микрофон + BlackHole 2ch)

3. **System Settings → Sound → Output** → выбрать Multi-Output Device

### Режимы записи

| Режим | Что записывает | Когда использовать |
|-------|----------------|-------------------|
| `mic` | Только микрофон | Локальная запись |
| `system` | Только системный звук | Запись вебинара |
| `both` | Микрофон + системный | **Онлайн-встречи** ✅ |

```bash
# По умолчанию: both (для встреч)
python3 -m meeting_transcriber record "Zoom"

# Только микрофон
python3 -m meeting_transcriber record "Заметка" --capture-mode mic

# Только системный звук
python3 -m meeting_transcriber record "Вебинар" --capture-mode system
```

## 🎯 Backends транскрипции

| Backend | Скорость | Качество | Требования |
|---------|----------|----------|------------|
| `groq` | ⚡ Очень быстро | Отлично | GROQ_API_KEY |
| `auto` | ⚡ Быстро | Отлично | GROQ_API_KEY (fallback на локальный) |
| `faster` | 🐢 Медленно | Отлично | CPU/GPU |
| `whisper` | 🐢 Медленно | Отлично | CPU/GPU |
| `whisperx` | 🐢 Медленно | Отлично + диаризация | GPU, HF_TOKEN |

### Groq API (рекомендуется)

Бесплатно до 8 часов аудио в день!

```bash
# Получить ключ: https://console.groq.com
export GROQ_API_KEY="gsk_xxx"

# Использовать
python3 -m meeting_transcriber transcribe file.wav --backend groq
```

## 🧠 Суммаризация

Генерирует краткое саммари встречи через LLM (Llama 3 через Groq).

```bash
# С суммаризацией
python3 -m meeting_transcriber transcribe file.wav --summarize

# Авто-суммаризация (всегда)
export AUTO_SUMMARIZE=1
```

Результат:
- `*_summary.md` — форматированное саммари
- `*_summary.json` — структурированные данные

## 🎭 Диаризация спикеров

Определяет кто что сказал (требует WhisperX и HuggingFace токен).

```bash
# Установка
pip3 install whisperx

# Токен: https://huggingface.co/settings/tokens
export HF_TOKEN="hf_xxx"

# Использование
python3 -m meeting_transcriber transcribe file.wav --diarize --speakers 3
```

## ⚙️ Конфигурация

Все параметры можно задать через переменные окружения:

### Основные

| Переменная | Значения | По умолчанию | Описание |
|------------|----------|--------------|----------|
| `CAPTURE_MODE` | mic/system/both | both | Режим захвата |
| `ASR_BACKEND` | faster/whisper/whisperx/groq/auto | faster | Backend |
| `WHISPER_MODEL` | tiny/base/small/medium/large-v2 | medium | Модель Whisper |
| `FORCE_RU` | 0/1 | 0 | Принудительно русский язык |

### Groq API

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `GROQ_API_KEY` | — | API ключ |
| `GROQ_MODEL` | whisper-large-v3 | Модель Whisper |
| `ASR_FALLBACK` | 1 | Fallback на локальный при ошибке |

### Суммаризация

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `AUTO_SUMMARIZE` | 0 | Авто-суммаризация |
| `SUMMARIZER_MODEL` | llama-3.3-70b-versatile | LLM модель |

### Диаризация

| Переменная | Описание |
|------------|----------|
| `HF_TOKEN` | HuggingFace токен |
| `DIARIZE_MIN_SPEAKERS` | Мин. число спикеров |
| `DIARIZE_MAX_SPEAKERS` | Макс. число спикеров |

## 📋 Команды CLI

```bash
# Справка
python3 -m meeting_transcriber --help
python3 -m meeting_transcriber record --help
python3 -m meeting_transcriber transcribe --help

# Запись
python3 -m meeting_transcriber record "Название" [опции]
  --capture-mode {mic,system,both}  # Режим захвата
  --device DEVICE                   # ID устройства или 'blackhole'
  --filter-preset {raw,soft,full}   # Пресет аудио фильтров
  --backend {groq,auto,faster}      # Backend транскрипции
  --no-transcribe                   # Только запись
  --diarize                         # Определять спикеров
  --speakers N                      # Число спикеров
  --summarize                       # Генерировать саммари
  --no-summarize                    # Отключить суммаризацию
  --no-monitor                      # Отключить индикатор уровня

# Транскрипция
python3 -m meeting_transcriber transcribe FILE [опции]
  --backend {groq,auto,faster,whisper,whisperx}
  --diarize                         # Определять спикеров
  --speakers N                      # Число спикеров
  --summarize                       # Генерировать саммари
  --no-summarize                    # Отключить суммаризацию
  --summary-lang {ru,en}            # Язык саммари
  --no-filter                       # Отключить фильтрацию галлюцинаций
  --no-fallback                     # Отключить fallback на локальный backend

# Утилиты
python3 -m meeting_transcriber list-devices      # Список устройств
python3 -m meeting_transcriber blackhole-status  # Статус BlackHole
python3 -m meeting_transcriber blackhole-status --setup  # Инструкции
```

## 🔧 Troubleshooting

### Нет звука собеседников

1. Проверь: System Settings → Sound → Output → **Multi-Output Device**
2. Запусти: `python3 -m meeting_transcriber blackhole-status`

### Транскрипция зависает

```bash
export FASTER_CPU_THREADS=1
export ASR_DEVICE=cpu
```

### Groq ошибка лимита

Бесплатный лимит: 28,800 секунд/день. При превышении автоматически переключится на локальный backend.

### Диаризация не работает

1. Установи: `pip3 install whisperx`
2. Токен: `export HF_TOKEN="hf_xxx"`
3. Прими лицензию: https://huggingface.co/pyannote/speaker-diarization-3.1

## 📦 Зависимости

**Обязательные:**
- Python 3.9+
- ffmpeg (`brew install ffmpeg`)
- faster-whisper

**Опциональные:**
- BlackHole (`brew install blackhole-2ch`) — запись системного звука
- sounddevice, numpy — мониторинг уровня
- groq — облачная транскрипция
- whisperx — диаризация спикеров

## 📄 Лицензия

MIT

## 🎨 Новый CLI интерфейс

С версии **v5.6.0** CLI полностью переписан с использованием **Typer** и **Rich**:

- ✨ Красивые таблицы с информацией о настройках
- 🎨 Цветной вывод с иконками
- 📋 Панели и структурированный вывод
- ✅ Информативные сообщения об ошибках
- 🚀 Автоматические проверки доступности API

**Пример вывода:**

```
Backend         🚀 Groq API (облако)
Files           1 файл(ов)
Диаризация      🎭 Включена
  Спикеров      3
Суммаризация    🧠 Включена (язык: ru)
```

## 🔄 Версия

**v5.6.0** — Modern CLI с Typer + Rich

